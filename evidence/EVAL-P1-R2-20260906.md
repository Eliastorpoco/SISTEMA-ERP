# EvaluaciónCompetencia P1 — recuperación y cierre R2

Se continuó desde el worktree R1 sin repetir recovery. Fuente ya identificada:
commit `7b6f0ce` (frontend Academic Core sin datos fallback), migraciones históricas
`077d0387fcec` y `086dda7dd191`, contrato AD/A/B/C y puntaje opcional 0–20.
Estado inicial ACTIVE_PARTIAL; `evaluaciones_ia` es log técnico, no registro oficial.

La entidad recuperada usa competencias → evaluaciones → resultados_evaluacion.
Periodo/sección/curso se derivan de la asignación activa; el resultado conserva
matricula_id, estudiante, competencia mediante evaluación, tenant, resultado,
creador/revisor y timestamps. Upsert único por tenant/evaluación/estudiante.
No se migraron logs IA a resultados oficiales ni se modificó Aula Virtual.
Las consultas propias conservan resultados históricos de matrículas previamente
válidas; nuevas escrituras y estudiantes elegibles exigen matrícula activa.

No existe aplicación FastAPI paralela desplegada. El controlador se incorpora en
`/app/controllers/evaluaciones_controller.py` al router principal.
El Dockerfile NO copia `backend_evaluacion/main.py` a `/app/main.py`.
`integrate_router.py` verifica el hash del main base y añade exclusivamente dos
líneas (import e include_router), conservando todos los demás bytes y appuser.
El snapshot local main.py de R1 permanece para comparación, fuera del empaquetado.

Main base SHA-256:
`3d12862322d44e92129cbc7239425c57d485046dbe8eb014d3e57b0b3abcf082`.
Main final/productivo SHA-256:
`57bf912d9382ff2cb6d9c643631c6507dd4b10f05a9b39838523d4151f8a1a32`.
Controlador final SHA-256:
`34eb7d6726cd0d52fb2efb3eecf68f12786b8b0383ebc757f708f63fa9a1d895`.

Comparación de aplicaciones reales importadas sin lifespan y con conexiones SQL
prohibidas, misma instrumentación en base y candidato:
BASE_ROUTE_COUNT=203; CANDIDATE_ROUTE_COUNT=210; MISSING_BASE_ROUTES=0.
Se comparan path, métodos y función del endpoint; detalle en EVAL-P1-R2-routes.json.
Routers anteriores de matrícula, horarios, asistencia, incidencias, apoderados,
finanzas y aula virtual preservados.

Validación focal `scripts/test-evaluacion-p1.py`: PASS mediante HTTP FastAPI contra
PostgreSQL efímero. ADMIN/DIRECTOR tenant actual; docente asignado y bloqueo ajeno;
estudiante solo propio; rechazo sin matrícula activa o sección correspondiente;
competencia/curso/asignación de otro tenant bloqueados; tenant extra en payload
rechazado; create/update conserva tenant y resultado único; consulta posterior
recupera lo persistido; cross-tenant rows=0; niveles y rango preservados.

Schema: se demostró necesaria la migración de las tres tablas históricas ausentes.
Primer intento productivo revertido transaccionalmente: la FK UUID a tenants.id
era incompatible con el registro actual varchar. Se verificó que no quedaron
tablas ni cambio de head. Se adaptó tenant_id al patrón UUID vigente del núcleo
académico, con guards de usuario/relaciones tenant-scoped y FKs compuestas entre
competencia, evaluación y resultado; no se alteró la tabla tenants.
Se restauró el schema real desde backup en PostgreSQL efímero y allí la migración
adaptada pasó; luego se repitió el harness focal con PASS antes de producción.
Migración productiva aplicada transaccionalmente; head `evaluacion_competencia_p1`.
La revisión también está empaquetada en `/app/alembic/versions_production`.

Backup previo: `backups/evaluacion-p1-r2-20260906T025500Z/pre-migration.dump`,
permisos privados, catálogo verificado con pg_restore --list.
SHA-256 `1d3c8140fb99d77a3a7248050148658044963d8b3d18c8dbdb7216b6125115ee`.
No hubo inserciones sintéticas ni pruebas de negocio mutables en producción.

Frontend: contrato canónico, selectores reales de asignación/competencia,
matriculados elegibles, registro de nivel/puntaje/retroalimentación, lectura de
resultados persistidos. ADMIN/DIRECTOR gestionan competencias; DOCENTE opera en
sus asignaciones; ESTUDIANTE solo lectura propia. URL conserva la evaluación al
refrescar; no usa tenant del cliente ni registros simulados.
Build y lint focal PASS. `scripts/test-eval-frontend.mjs` PASS (SSR/servicio aislado,
roles de ruta, payload canónico, sin llamadas externas).
SEGUNDO_TENANT_VISIBLE=no; TENANT_HARDCODE_COUNT=0 en pantalla/servicio/bundle.
Sin npm ci ni instalación de Playwright. No se afirma una nueva sesión interactiva
de navegador: visibilidad validada por harness, build y artefactos públicos.

Smoke productivo de solo lectura: /evaluaciones, /matricula, /asistencia,
/horarios y /incidencias HTTP 200; index y bundle idénticos al candidato.
Smoke autenticado real GET /evaluaciones y /evaluaciones/catalogos HTTP 200,
actor existente y JWT efímero no expuesto; no se ejecutó ninguna mutation.

Backend estable: `erp-backend:evaluacion-p1-r2-20260906T025500Z`.
Frontend estable: `erp-frontend:evaluacion-p1-r2-20260906T025500Z`.
Rollback backend: `erp-backend:evaluacion-p1-r2-rollback-20260906T025500Z`.
Rollback frontend: `erp-frontend:evaluacion-p1-r2-rollback-20260906T025500Z`.
Solo se recrearon backend/frontend; PostgreSQL, Redis, n8n y Evolution no se
recrearon. Rollback de aplicación conserva las nuevas tablas sin borrar datos;
una restauración del backup requeriría evaluar las escrituras posteriores.

Stage selectivo P1, conservando stage previo y cambios ajenos. Sin push.
EVAL_PRODUCTION_PASS=sí. Sin bloqueo. No se inició otro módulo.
